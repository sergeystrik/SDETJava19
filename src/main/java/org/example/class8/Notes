Break keyword:
  Break - breaks the loop.We always use break with if conditions once the condition is met
   it is stops the loop even if there are still iterations remaining.
   Why should we use Break:
    Break can save unnecessary processing

    Continue keyword:
    Continue is used to skip unnecessary calculations.
    if an element meets a condition that element is skipped and we move to the next element.
    Why should we use
to save resources
2D Arrays:
 2D Arrays is simply to pull or store information in a 2D array