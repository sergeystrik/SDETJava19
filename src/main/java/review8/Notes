FileHandling:
  Reading/writing data to files through programming is called FileHandling.
To read/write Data to file:
   1) location of the file'
   2) Navigate to the file.
   3) A suitable class to read/write data'
 How to get the location of file in intellij?
    1) Right click->copy path reference -> Absolute path
How do we navigate ?
   We can use FileInputStream class to do navigation.
Why do testers use configuration files ?
   So that we can store sensitive info like usernames and passwords outside of our code.
Why do testers use Excel files?
  As a tester wew store testing data in Excel file.We store all of our data performing data-driven test cases